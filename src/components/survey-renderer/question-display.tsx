"use client";

// Placeholder: Renders a single question with the appropriate input control
// based on its type (multiple choice, free text, rating scale, etc.)
export function QuestionDisplay() {
  return <div>Question display placeholder</div>;
}
